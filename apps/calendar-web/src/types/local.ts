/**
 * Local types for web app
 * For backward compatibility with older components
 */

export interface DateOption {
  id: string;
  date: string; // ISO date format: "2025-01-10"
  label: string; // Display format: "Fri Jan 10"
}
